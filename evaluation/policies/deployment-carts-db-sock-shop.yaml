apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deployment-carts-db-sock-shop
  namespace: sock-shop
spec:
  egress: []
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: sock-shop
      podSelector:
        matchLabels:
          name: carts
    ports:
    - port: 27017
  podSelector:
    matchLabels:
      name: carts-db
  policyTypes:
  - Ingress
  - Egress
